{
  "name": "poco",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --network mumbai poco-dapp/poco",
    "create-local": "graph create --node http://localhost:8020/ poco-dapp/poco",
    "remove-local": "graph remove --node http://localhost:8020/ poco-dapp/poco",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 --network mainnet poco-dapp/poco",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.33.0",
    "@graphprotocol/graph-ts": "0.27.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
